{
  "name": "mop-plugins",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Chillizu/mop-plugins.git"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "node --import ./test/register-mocks.mjs --test \"test/*.test.js\"",
    "check": "node --check packages/mop-tool-recovery/index.js && node --check packages/mop-magic-keywords/index.js && node --check packages/mop-capabilities/index.js && node --check packages/mop-executor/index.js && node --check packages/mop-learn/index.js && node --check packages/mop-model-auth/index.js",
    "lint": "eslint .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0",
    "globals": "^15.0.0",
    "prettier": "^3.0.0"
  }
}
