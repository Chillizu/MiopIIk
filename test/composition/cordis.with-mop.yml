# The full fixture (tools/session/agent/subagent/subagent-spawn) + all three
# Config-schema mop packages. Each carries no config here, so composition.test.mjs
# asserts each Config schema's defaults land in apply's second arg. This is what
# catches the mock-stub-masked contract bugs (z.number().int, z.string().optional).
- id: session
  name: '@deepseek-ai/dsh-session'

- id: agent
  name: '@deepseek-ai/dsh-agent'

- id: system-prompt
  name: '@deepseek-ai/dsh-system-prompt'
  config:
    persona: ''

- id: tools
  name: '@deepseek-ai/dsh-tools'

- id: subagent
  name: '@deepseek-ai/dsh-subagent'

- id: subagent-spawn-in-process
  name: '@deepseek-ai/dsh-subagent-spawn-in-process'

- id: mop-executor
  name: '@chillizu/mop-executor'

- id: mop-magic-keywords
  name: '@chillizu/mop-magic-keywords'

- id: mop-model-auth
  name: '@chillizu/mop-model-auth'
